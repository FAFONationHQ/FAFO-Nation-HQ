import Image from "next/image";
import { notFound } from "next/navigation";
import Header from "../../../Header";
import { getProduct } from "../../../../lib/catalog";
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const { slug }=await params; const product=getProduct(slug); if(!product) notFound(); return <main className="min-h-screen bg-black text-white"><Header /><section className="mx-auto grid max-w-7xl gap-10 px-5 py-16 lg:grid-cols-2"><Image src={product.image.src} alt={product.image.alt} width={1200} height={1200} className="w-full bg-neutral-950 object-contain" /><div><p className="text-xs font-black uppercase tracking-[.18em] text-red-500">{product.collection.replaceAll("-", " ")}</p><h1 className="mt-5 text-4xl font-black uppercase leading-tight">{product.title}</h1><p className="mt-6 text-2xl font-bold text-[#D4AF37]">{product.priceCad}</p><p className="mt-6 leading-8 text-white/60">Purchase is completed through Printify. Price shown is the public Printify price when this catalog was verified.</p><a href={product.purchase.url} target="_blank" rel="noopener noreferrer" className="mt-8 inline-flex min-h-12 items-center border border-red-600 px-6 text-xs font-black uppercase tracking-[.16em]">Continue to Printify ↗</a></div></section></main>; }
