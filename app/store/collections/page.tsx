import Link from "next/link";
import Header from "../../Header";
import { catalogCollections, publishedProducts } from "../../../lib/catalog";
export default function CollectionsPage() { return <main className="min-h-screen bg-black text-white"><Header /><section className="mx-auto max-w-7xl px-5 py-16"><p className="text-xs font-black uppercase tracking-[.28em] text-red-500">FAFO Nation Store</p><h1 className="mt-4 text-5xl font-black uppercase">Collections</h1><div className="mt-10 grid gap-4 md:grid-cols-2">{catalogCollections.map((collection) => { const count=publishedProducts.filter((p)=>p.collection===collection.slug).length; return <Link key={collection.slug} href={`/store/collections/${collection.slug}`} className="border border-white/15 p-6 transition hover:border-[#D4AF37]/60"><p className="text-xs font-black uppercase tracking-[.18em] text-[#D4AF37]">{count ? `${count} approved listings` : "Future collection"}</p><h2 className="mt-3 text-2xl font-black uppercase">{collection.name}</h2><p className="mt-3 text-white/60">{collection.description}</p></Link>})}</div></section></main>; }
