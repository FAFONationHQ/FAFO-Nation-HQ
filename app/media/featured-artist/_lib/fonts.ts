import localFont from 'next/font/local';

export const secretSolver = localFont({ src: '../../../../public/fonts/Secret Solver.otf', display: 'swap', fallback: ['Arial Black', 'Impact', 'sans-serif'] });
